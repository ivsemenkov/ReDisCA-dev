# ReDisCA Examples

Run from the repository root after installing the package:

```bash
python examples/basic_usage.py
```

`basic_usage.py` fits the current `ReDisCA` estimator on a small synthetic
condition-average tensor and a target RDM, then calls `transform` and
`inverse_transform`.
