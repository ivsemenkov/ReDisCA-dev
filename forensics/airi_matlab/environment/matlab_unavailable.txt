MATLAB is not installed in this Cloud Agent environment.

Phase A `which -all` for spoc, whiten_data, create_Cxxz, and
random_phase_surrogate was therefore not executed.

When MATLAB is available, run:

    scripts/record_matlab_environment.m

and replace this file with matlab_which_all.txt plus SHA-256 hashes of
each resolved path.
