This directory holds outputs of the **literal** AIRI sensor-space run.

`data_inspection.json` and `static_reconstruction.json` were produced without MATLAB
from OSF file headers and the pinned AIRI source. They are not SPoC outputs.

`airi_literal_after_spoc.mat` will appear only after MATLAB executes
`scripts/run_literal_sensor_space.m`.
