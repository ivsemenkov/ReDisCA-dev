The source-audit worker writes the full inventory here after reading the
published paper and pinned AIRI/SPoC sources. Track workers must wait for
that file (and `reproduction_manifest.json`) before claiming a figure is in
or out of scope.
