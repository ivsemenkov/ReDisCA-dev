# Simulations track

Owner: simulations worker. Do not edit `paper/reproduction/common/`.

Reproduce all simulation sections of Ossadtchi et al. 2024, not a toy
benchmark. If a required forward/source model is unavailable, label results
`blocked` or `approximate` rather than silently substituting fsaverage.

Compact metrics go to `paper/results/simulations/`.
